# ============================================
# MySQL Virtual Alias Maps
# Query virtual aliases from database
# Returns destination addresses for alias
# ============================================

user = ${DB_USER}
password = ${DB_PASSWORD}
hosts = ${DB_HOST}:${DB_PORT}
dbname = ${DB_NAME}
query = SELECT destination FROM aliases WHERE source='%s' AND active=1